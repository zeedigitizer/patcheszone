All interface icons are provided by Font Awesome 6 (Free), loaded via CDN in
every page's <head>. No local icon files are required. Custom brand marks
(logo, favicon) live in the /images folder as SVG/PNG/ICO.
